{
  "version": 2,
  "builds": [{ "src": "./dist/index.js", "use": "@vercel/node" }],
  "rewrites": [{ "source": "/api/(.*)", "destination": "./dist/api" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./dist/index.js"
    }
  ]
}
